#include <vector>

#ifndef LIMIT
#define LIMIT 1000
#endif

int problem1(int value[3,5]){

  vector<int> result;

  for (int i = 1; i < LIMIT; i++) {
    result.push_back(i * 3,i * 5);
  }

  return x;
}
